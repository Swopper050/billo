apiVersion: apps/v1
kind: Deployment
metadata:
  name: billo-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billo-redis
  template:
    metadata:
      labels:
        app: billo-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          args: ["redis-server", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: billo-redis
spec:
  type: ClusterIP
  selector:
    app: billo-redis
  ports:
    - port: 6379
      targetPort: 6379
