apiVersion: apps/v1
kind: Deployment
metadata:
  name: billo-tasks-beat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billo-tasks-beat
  template:
    metadata:
      labels:
        app: billo-tasks-beat
    spec:
      imagePullSecrets:
        - name: billo-dockerhub
      containers:
        - name: billo-tasks-beat
          image: <dockerhub_namespace>/billo-tasks
          command: ["celery"]
          args:
            [
              "-A",
              "server.celery_app",
              "beat",
              "--loglevel=info",
              "--schedule=/var/run/celery/celerybeat-schedule",
            ]
          envFrom:
            - configMapRef:
                name: billo-backend-config
            - secretRef:
                name: billo-backend-secrets
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          volumeMounts:
            - name: celerybeat-schedule
              mountPath: /var/run/celery
      volumes:
        - name: celerybeat-schedule
          emptyDir: {}
